FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .

EXPOSE 5173

ENV VITE_API_PROXY_TARGET=http://server:3001

CMD ["npm", "run", "dev:client", "--", "--host", "0.0.0.0", "--port", "5173"]
